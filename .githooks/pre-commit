#!/usr/bin/env sh
set -eu

repo_root="$(git rev-parse --show-toplevel)"
cd "$repo_root"

if command -v uv >/dev/null 2>&1; then
    uv run ruff check .
    uv run ty check
    uv run pyright
    exec uv run pytest
fi

exec python3 -m pytest
